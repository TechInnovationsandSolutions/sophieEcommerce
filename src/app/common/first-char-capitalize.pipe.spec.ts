import { FirstCharCapitalizePipe } from './first-char-capitalize.pipe';

describe('FirstCharCapitalizePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCharCapitalizePipe();
    expect(pipe).toBeTruthy();
  });
});
